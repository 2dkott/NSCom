using UnityEngine;
using System.Collections;

public class AddingParticleSystem : MonoBehaviour {

    public bool isMechShape;
    public ParticleSystem particalSystem;

    void Awake()
    {
        if (isMechShape)
        {
        }
    }
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
